#pragma once
#include <lvgl.h>

/**
 * DeepSeek whale icon — 16×16 pixel 1-bit image.
 *
 * Generated manually as a simple whale silhouette. Displayed as a small
 * icon on the idle screen, e.g. above the clock or beside the title.
 *
 * The LVGL image header uses the built-in compressor-free format so it
 * compiles without any external tool.
 */
extern const lv_img_dsc_t whaleIcon;