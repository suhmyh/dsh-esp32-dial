修复云端编译错误

三处，都是云端构建替我发现的（本机 PlatformIO 装不上，无从发现）：

- Provision.cpp: raw string 定界符改为 R"html(...)html"。内嵌 HTML 里
  onsubmit="return save(event)" 结尾的 )" 提前终止了 R"(...)"，导致后面
  所有 C++ 代码被当作 HTML 解析 —— 一处笔误引发 20 多条错误。
- Provision.cpp: esp_efuse.h 换成 esp_mac.h，后者才声明
  esp_efuse_mac_get_default()。
- DialUi: LVGL 事件回调从匿名 namespace 函数改为 static 成员函数。
  匿名 namespace 里的函数无法访问 private 的 onAskButton()，且
  buildAskOverlay() 在其定义之前就引用了它。
