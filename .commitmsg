修复中文显示为方块，并重新设计配网页面

字体问题: 圆盘上中文全是方块的根因
- lv_conf.h 启用 LV_FONT_SIMSUN_16_CJK。Montserrat 是纯英文字体
  不含任何汉字字形，所有中文标签都画成空心方块。
- 所有承载中文的 label 改用 lv_font_simsun_16_cjk: 会话标题、详情
  审批标题与正文、允许/拒绝按钮、配网提示。
- 相位大图标改用 LVGL 内置符号 (LV_SYMBOL_OK / CLOSE / WARNING /
  REFRESH / MINUS / EYE_CLOSE)。原先的勾叉直径省略号同样不在字体里。
- 只画 ASCII 的 label (AP 名、密码、门户 URL) 保留 Montserrat。

配网页面重新设计: 原先打开后看不出要做什么
- 拆成三步卡片: 让圆盘上网 / 连接电脑上的桥接 / 保存。
- 每个字段说明填什么、从哪里取: ipconfig 看 IPv4、桥接窗口复制
  token、圆盘只支持 2.4G。
- 扫描按钮改称查看附近，提交按钮改称保存并连接。
- 保存过程有进行中状态以及明确的成功/失败反馈。

圆盘配网界面加上手机扫码连接指引，AP 名与密码并排一行显示。
